import { Meta, ArgsTable } from "@storybook/addon-docs";
import StoryAndSource from "./components/StoryAndSource";
import ClickAwayListener from "../ClickAwayListener";

<Meta title="Basic usage" />

# Basic usage

Clicking outside of the `<ClickAwayListener>` content triggers the `onClickAway` callback.

<StoryAndSource id="stories-basic--scenario-1" />

<ArgsTable of={ClickAwayListener} />
